# Gradle / Android Studio build outputs
.gradle/
build/
/app/build/
/local.properties
/.idea/
*.iml
captures/

# Wrapper jar is generated by Android Studio on first sync; the .properties
# file IS committed (when present) so Studio knows which gradle version.

# OS noise
.DS_Store
Thumbs.db
